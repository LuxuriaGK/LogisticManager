Module LogisticCalculation
    Private arr_Logistic(60) As LogisticSupport
    Private filteredLogistic(60) As LogisticSupport


    Public Sub setLogisticData(logistic() As LogisticSupport)
        arr_Logistic = logistic
    End Sub

    Private Sub getRanking(mChecked As Boolean, aChecked As Boolean, rChecked As Boolean, pChecked As Boolean)
    End Sub



End Module
